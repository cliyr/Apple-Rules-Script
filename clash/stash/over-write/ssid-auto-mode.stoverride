name: Auto Swicth Proxy
desc: 自动切换代理
# 必须搭配专门的 Stash 订阅转换配置才可以正常使用
proxy-groups:
  - name: 🚦 SSID Auto Mode
    type: select
    proxies:
    # 此处为配置文件中的已有策略组
      - 🚀 节点选择
      - DIRECT
    ssid-policy:
    # cellular: 保留字，指代蜂窝数据条件下
    # default：保留字，指代其他条件下
      HedyLamarr: DIRECT
      cellular: 🚀 节点选择
      default: 🚀 节点选择